<div class="container">
  <h3 class="bg-secondary text-white d-inline-block px-4 ml-5">投稿内容確認</h3>

  <div class="container my-4">
    <h4>ご自身の訪れた国名を選択（必須）</h4>
    <div class="row">
      <%= @review.country_code %>
    </div>

    <h4>ご自身の訪れた地域を入力（必須）</h4>
    <div class="row">
      <%= @review.area %>
    </div>

    <h4>本経験の評価（必須）</h4>
    <div class="form-group" id="rating-form">
      <%= f.hidden_field :rate, id: :review_star %>
    </div>

    <script>
    $(document).on('turbolinks:load', function() {
      $('#rating-form').empty();
      $('#rating-form').raty({
        size: 36,
        starOff: '<%= asset_path('star-off.png') %>',
        starOn : '<%= asset_path('star-on.png') %>',
        starHalf: '<%= asset_path('star-half.png') %>',
        scoreName: 'review[rate]',
        half: true,
      });
    })
    </script>

    <h4>タイトル</h4>
    <div class="row">
      <%= @review.title %>
    </div>

    <h4>口コミの入力（必須）</h4>
    <div class="row">
      <%= @review.body %>
    </div>

    <h4>出国した国名を選択（任意）</h4>
    <div class="row">
      <%= @review.departure %>
    </div>

    <h4>往復の移動費用（任意）</h4>
    <div class="row">
      <%= @review.travel_cost %>円
    </div>

    <h4>宿泊費用（任意）</h4>
    <div class="row">
      <%= @review.accommodation_fee %>円
    </div>

    <h4>宿泊日数（任意）</h4>
    <div class="row">
      <%= @review.night %>
    </div>

    <h4>写真をアップロード（任意）</h4>
    <div class="row">
      <%= @review.travel_images %>
    </div>

    <div class="row justify-content-center mt-4">
      <%= f.submit "投稿確認画面へ進む", class: "btn btn-success mx-2 px-5" %>
    </div>
  </div>

</div>