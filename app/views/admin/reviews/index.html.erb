<div class="container">
  <div class="row">
    <h1 class="font-weight-normal mt-3">投稿一覧</h1>
  </div>
  <div class="row">
    <%= render 'admin/reviews/tag_index', tag_list: @tag_list, reviews: @reviews %>
  </div>
  <%= render 'admin/reviews/index', reviews: @reviews %>
</div>